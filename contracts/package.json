{
  "name": "alphatrace-contracts",
  "version": "1.0.0",
  "description": "AlphaTrace smart contracts for 0G Chain",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network og_mainnet",
    "deploy:testnet": "hardhat run scripts/deploy.js --network og_testnet",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
